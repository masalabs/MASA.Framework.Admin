@inject NavigationManager Navigation

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        //Navigation.NavigateTo("/pages/authentication/Login-v2", true);
        Navigation.NavigateTo($"account/login", true);
    }
}
