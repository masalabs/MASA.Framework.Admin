@page "/"
@inherits LayoutComponentBase
@inject NavigationManager Nav

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if(firstRender)
        {
            Nav.NavigateTo("/blog-admin/article",true);    
        }
    }
}
